# 当前 Idea：历史条件下的关联诱发局部可塑性

更新：2026-09-09。当前修订来源为用户提供的 [Pro 理论报告](D:/THEORY_REPAIR_AND_TASK_CONTRACT_20260909.md)及配套研究答复。核心算子与轻量数值核验已落实，并完成匹配自身修正量的条件合成对照；本轮未再改动机制。Habitat/HM3D 模拟与真实视觉验证继续暂缓。

## 1. 单一主旨与固定任务

**让区室保留经历，让区室—胞体的互易传输比较当前历史与候选经验的兼容性，再把“建立关联引起的局部传输变化”乘上新到达证据写入持久参数。**

研究对象固定为陌生静态室内环境中的在线跨视角地点关联：在相似观测和不同运动历史下，用实际后继消歧，并检查交错经历后的保持及未用于更新的新视角迁移。导航提供视觉—动作经验；当前没有转向物体任务、操控或重建。

候选贡献是“神经结构如何实现关系特异的信用计算”。局部梯度公式、能量对比、四个区室或三次非线性各自不构成新颖性。结构是否带来更好的纠错—保持能力，仍需同信息、同预算对照证明。

## 2. 本次修改及其连续性

保留四区室与一个胞体、实际 incoming、互易电流、四个可塑传输参数和后继到达后的一次写入。原历史门控 \(w,C_*\) 及三路余弦读出退出当前候选，旧代码与数值作为回归记录保留。

新关联读取是两份历史在固定区室端口连接下的**净耦合代价**；新资格来自连接前后局部量之差。这样，关联分数和突触资格对应同一个计算对象。旧算子不能只换读出名称就满足这个定义，必须增加配对求解并完整扣除自由能量；该变化在现有互易主流上实现。

参数 \(\theta\) 控制区室—胞体的非线性传输，持续影响后续前向。电位是动态状态，候选记录是有限缓存；三者费用和作用分别核算。这里不以“快慢参数更新”作为方法主旨。

## 3. 自由电路：历史成为条件边界

每条记录 \(\xi\) 保存输入 \(x_\xi=(a_\xi,b_\xi)\) 和实际输入前状态 \(Z_\xi^-\)。定义
\[
Z=(z_1,\ldots,z_4,s),\quad d_j=z_j-s,\quad
\alpha_j=k\exp(\theta_j),\quad I_j=kd_j+\alpha_jd_j^3.
\]
固定输入接线与泄漏为
\[
V(a,b)=(a,-a,-b,b),\quad m(a,b)=(b,-b,a,-a),\quad
L_{\xi j}=\ell+\epsilon m_j(x_\xi).
\]
要求 \(h,k,\ell_s,L_{\xi j}>0\)，输入、状态和参数有限，且计算所得 \(\alpha_j\) 严格正且有限。实现应拒绝指数溢出、下溢至零或不收敛，不静默裁剪。

条件单步能量：
\[
U_\xi(Z;\theta)=
\frac{\|Z-Z_\xi^-\|^2}{2h}
+\frac12\sum_j L_{\xi j}z_j^2+\frac{\ell_s}{2}s^2
+\sum_j\left(\frac{k}{2}d_j^2+\frac{\alpha_j}{4}d_j^4\right)
-(V(x_\xi),0)^TZ.
\]
记 \(\bar Z_\xi=\arg\min_Z U_\xi(Z;\theta)\)，\(\nu_\xi=U_\xi(\bar Z_\xi;\theta)\)。驻点等价于原自由隐式步：
\[
\bar Z_\xi=Z_\xi^-+hF_\theta(\bar Z_\xi,x_\xi),\quad
F_j=-L_{\xi j}z_j-I_j+V_j,\quad F_s=-\ell_s s+\sum_j I_j.
\]
正泄漏与正微分电导使 Hessian 正定，能量强制增长，因此自由态唯一。内部电流守恒并耗散，外部驱动仍可供能。一个隐式 token 步不等于整段连续时间轨迹。

## 4. 关联读取：两份经历连接后的额外代价

固定端口 \(S=[I_4\;0]\) 读取聚合前区室电位，桥接强度 \(\gamma>0\)。每次比较保留查询 \(q\) 与候选 \(C\) 各自的 incoming：
\[
E_{qC}^*=\min_{Z,W}
\left[U_q(Z)+U_C(W)+\frac{\gamma}{2}\|SZ-SW\|^2\right].
\]
两端桥接电流分别为 \(-\gamma S^T(SZ-SW)\) 和其相反数。配对最优态记 \(Z_{q|C},Z_{C|q}\)。定义
\[
\boxed{\mathcal A_\theta(q,C)=E_{qC}^*-\nu_q-\nu_C},\qquad
\boxed{f=\frac{\mathcal A(q,B)-\mathcal A(q,A)}{\tau_E}},\quad \pi=\sigma(f).
\]
\(\tau_E>0\) 是事前固定的能量单位。代价小表示历史条件下更兼容，不自动等于同一地点。

精确解满足：\(\mathcal A\ge0\)；交换完整记录后不变；\(\mathcal A=0\) 当且仅当两个自由态的 \(S\) 端口一致；独立的、参数相关但状态无关能量偏置 \(U_\xi+b_\xi(\theta)\) 不改变关联代价及其梯度。配对 Hessian 是两个正定块加半正定桥接项，因此配对解也唯一。

端口从二维聚合读出变为四维区室电位增加了可见信息。普通基线必须获得相同端口，不能把这一信息增量当作胞体独有贡献。

## 5. 局部资格：关联引起了哪些传输变化

定义本地量
\[
B_j(Z)=\partial_{\theta_j}U_\xi(Z;\theta)
=\frac{\alpha_j}{4}d_j^4
=\frac14d_j I_j^{\mathrm{nonlinear}}.
\]
精确最小值处，包络定理消去状态导数，得到
\[
\boxed{e_{C,j}=
B_j(Z_{q|C})+B_j(Z_{C|q})-B_j(\bar Z_q)-B_j(\bar Z_C)},
\qquad
\boxed{g_j=\partial_{\theta_j}f=(e_{B,j}-e_{A,j})/\tau_E}.
\]
查询和候选两侧都依赖同一 \(\theta\)，四项均需保留。条件偏导固定实际 incoming，不反传其历史生成链。新求解器通过局部量读取信用，不需要原余弦读出的显式 adjoint；状态求解本身仍有计算成本。

有限求解残差下，该公式是精确资格的近似。核验必须比较多个有限差分步长与更严格求解容差，报告绝对误差和非零信用尺度，不能仅用接近零的向量证明一致。

## 6. 后继教师：有相对证据，还需候选覆盖

在后继 \(y\) 到达前固定候选预测密度 \(P_A,P_B\)、候选集外参考密度 \(P_\varnothing\) 和先验 \(\pi_0\in(0,1)\)。它们基于合法观测—动作信息，在本次 \(\theta\) 偏导中冻结，定义在相同观测空间与测度上。
\[
M=\pi P_A(y)+(1-\pi)P_B(y),\qquad
Z^{\mathrm{ev}}=(1-\pi_0)M+\pi_0P_\varnothing(y),\qquad
L=-\log Z^{\mathrm{ev}}.
\]
\[
\pi^+=\sigma\!\left(f+\log P_A(y)-\log P_B(y)\right),\quad
\omega=\frac{(1-\pi_0)M}{Z^{\mathrm{ev}}},\quad
\boxed{\delta=\omega(\pi^+-\pi)}.
\]
由 \(\partial_f L=-\delta\)，最终
\[
\boxed{\Delta\theta_j=\eta\delta g_j
=\frac{\eta\delta}{\tau_E}(e_{B,j}-e_{A,j})}.
\]

若 \(P_A=P_B\)，严格零写入。若候选解释相对 \(P_\varnothing\) 很弱，\(|\delta|\le\omega\) 随之减小。它依赖密度质量，不能保证所有语义错配零更新；所有密度都错时仍可能错误学习。实现使用 log 密度和 log-sum-exp，避免直接相乘的小概率下溢。

原 Gaussian 增量预测 \(P_C(y)=\mathcal N(y;x_q+(y_C-x_C),\Sigma_u)\) 只是待验证实例。相同动作不保证同位移，相似增量也不保证同地点。当前数值测试中的合法固定参考密度不等于已建立真实视觉的拒识模型。

## 7. 在线契约及费用

单个 pending 的顺序固定为：完成历史记录 → 冻结查询、候选、密度和参数版本 → 求解自由/配对态并缓存 \(f,g\) → 实际动作与后继到达 → 一次消费并更新 \(\theta\) → 处理下一观测。

等待后继期间不允许其他参数写入。只比较版本号不够，缓存还要绑定实际参数值；重复到达、候选未完成或版本不符应拒绝。配对态只存在于比较工作区；live 只推进真实观测的自由态，不提交候选探测轨迹。候选选择规则、缓存容量和到达记录均需显式界定。

每单元持续状态5个、可塑参数4个。一次 A/B 关联需要三个自由态和两次配对求解；查询自由态可以复用。每对有10个暂态电位，资格4个数，incoming、候选与日志额外计费。二维端口仅为算术实例；真实特征通过事前固定映射分成 \(M\) 组时，是 \(5M\) 个 live 状态及 \(4M\) 个参数，不能继续宣称整个视觉模型只有五个状态。

## 8. 能证明的选择性与尚待证明的能力

当 \(e_{A,j}=e_{B,j}\)，该连接本次不写入，即使其原始活动很大。这是候选相对的连接选择性，不是所有无关语义知识均被保护的定理。

另一项固定条件关联 \(f_\nu\) 的变化满足局部界
\[
|\Delta f_\nu|\le
\eta|\delta|\,|g_\nu^Tg|
+\frac{L_\nu}{2}\eta^2\delta^2\|g\|^2,
\]
其中 \(L_\nu\) 约束写入邻域内的 Hessian。因此长期保持仍需要小信用重叠、受控步长和未来状态漂移评估。四参数的有限维容量限制仍然存在。

在自由态附近令 \(v=S\bar Z_q-S\bar Z_C\)，\(\mathcal H_\xi=\nabla^2U_\xi(\bar Z_\xi)\)。二次型电路精确满足
\[
\mathcal A=\frac12v^T
[\gamma^{-1}I+S\mathcal H_q^{-1}S^T+S\mathcal H_C^{-1}S^T]^{-1}v.
\]
对当前四次能量这是局部近似。胞体通过逆 Hessian 改变支路比较电流的分配，是明确的结构作用；是否形成有用的选择性优势仍待对照。小 \(\gamma\) 极限退为完整端口上的平方距离，固定二次型退为有效度量。

## 9. 当前证据位置与接续目标

当前代码修改及实际核验结果统一记录在 [本轮实现与验证记录](D:/EV-TTT/refine-logs/RELATION_ENERGY_IMPLEMENTATION_20260909.md)。原共享门控的 \(0.021/0.024/0.024\) 是旧候选基点预言，原约97.48%回撤属于旧S，不转借为本式结果。

本轮已实现能量读取、四项局部资格、log 域覆盖教师和单 pending 条件写入。18组轻量检查通过；固定非零参数和暖历史下，局部资格与有限差分的最大分量误差约 \(1.69\times10^{-11}\)，教师损失导数误差约 \(4.28\times10^{-12}\)。这些结果支持数学接口和实现一致，不构成选择性保持、视觉有效性或新颖性证明。当前配对为十维直接 Newton 求解，连续观测驱动尚未接入。

接续的 [匹配修正量检验](D:/EV-TTT/refine-logs/RELATION_ENERGY_SELECTIVITY_20260909.md) 已覆盖冷、暖、暖交换候选×两种次序×四种实际后继组合，并保存原始分差、\(U/H/D\) 及绝对串扰。对照采用同自由算子、同四参数、同完整区室端口的平方距离，不代表全部普通模型。步长匹配是离线诊断，不是已接入的在线策略。

事前主目标 \(U=.001\)、单步参数欧氏距离上限1下，新机制没有一格完成两笔都匹配，对照为5/24格；因此主目标下尚不能比较保持优势。小尺度压力 \(U=.0001\) 下双方都匹配20/24格，新机制绝对串扰7格较低、13格较高，平均比率1.275940，对照1.269145，未显示系统改善。未匹配格没有删去，也没有降低主目标。

核心诊断更明确：六个固定基点上，新机制与距离基线的同历史梯度余弦为 .998771–.999997，范数比为 .498785–.544738；暖候选下新机制两项关联的梯度余弦仍为 .999143。当前配对计算主要改变了这些基点的资格尺度，尚未形成足够可区分的写入方向；这不是全局等价定理。下一步应解决候选相对资格为何仍共同变化，而不是继续用较小更新解释保持收益。

真实后继似然的地点辨识力、跨视角迁移、同地点不同路径归并、交错保持和相对普通模型的结构收益仍待闭合。当前可研究对象是历史条件的对比能量关联学习；“生物结构诱导能力形成”应由结构干预与独立行为证据支持。
