# 当前Idea：历史歧义下的区室—胞体选择性关联修正

## 1. 主旨与固定研究对象

研究问题是：已知的区室—胞体结构，能否通过直接参与前向的局部可塑性，在历史歧义下修正当前关联，同时少改动无关关联？同一地点可能经不同路径抵达，不同地点也可能外观相似；因此既要容纳共同关联，也要保留历史差异，不能只让一对相反教师恰好互助。候选贡献是可推导、可检验的结构—学习运算对应，不是发明生物原语或把旧模型移到新任务。

背景固定为Habitat/HM3D陌生静态室内导航中的在线跨视角空间情境关联，不转向ObjectNav、操控或一般重建。学习与候选选择仅允许RGB、实际执行动作和确实可测的自身状态；隐藏位姿、深度、重叠、地点标签、奖励及成功信息均不得进入这些接口。目前只有合成特征算术证据，模拟与真实渲染视觉验证已由用户暂缓。

视觉编码器、读出、接线和教师规则冻结；TTT更新四个传递参数 \(\theta_j\)，它们直接改变描述子前向，不是普通输入突触权重，也不以慢参数更新为主旨。四区室电位 \(z_j\) 与胞体电位 \(s\) 是在线状态；四区室及三次幂是当前建模选择，不是研究目标的硬约束。候选缓存和外部教学接口负责提供比较对象及后继证据，区室负责状态演化、描述子和参数响应；不要求所有运算都藏在一个细胞内，但每一部分的选择性贡献必须能够拆分归因。

## 2. 从历史状态到同一关联的共享门控

记三路 \(\xi\in\{t,A,B\}\) 分别为当前查询和两个已完成候选，各自保存输入前状态 \(Z_\xi^-=(z_{\xi,1}^-,\ldots,z_{\xi,4}^-,s_\xi^-)\)。当前二维输入 \(x=(a,b)\) 是冻结特征接口，不是动作；实际动作另记 \(u\)。固定驱动与调制接线为
\[
V(x)=(a,-a,-b,b),\qquad m(x)=(b,-b,a,-a),\qquad p=(1,-1,-1,1).
\]
极性 \(p\) 对应同一输入的成对正负接线，不读取历史编号、候选身份或教学符号。仅从查询incoming计算
\[
q_{t,j}=(z_{t,j}^- -s_t^-)^2,\quad
P_t=\tfrac14\sum_jq_{t,j},\quad
w_{t,j}=1+p_j(q_{t,j}-P_t)/C_*.
\]
校准在教学前、\(\theta=0\) 时，用最初两个已处理观测token之后的状态计算 \(q_j^*,P^*\)，冻结
\[
C_*=\sqrt{\tfrac14\sum_j(q_j^*-P^*)^2}>0.
\]
它不使用未来后继，也不随教学结果调节；\(C_*=0\) 时此定义不成立，不补造替代规则。

当前方案的实际接口变更是：三路共享查询的 \(w_t\)，但候选仍各自从自己的incoming演化，三路偏导全部保留。这是在“当前经历支持哪个记录”的同一次比较中统一参数作用的上下文，不是把候选状态换成查询，也不是人工分配历史身份。常量1保留共同写入，中心化项引入历史对比。\(P_t\) 是额外平方汇总与广播，不能说原胞体电位 \(s\) 自动算出了均值池。

## 3. 门控进入互易电流、隐式状态与读取

对每路定义压差 \(d_j^\xi=z_j^\xi-s^\xi\)，共享系数真正进入主前向：
\[
\alpha_{t,j}=k\exp(\theta_jw_{t,j}),\qquad
I_j^\xi=kd_j^\xi+\alpha_{t,j}(d_j^\xi)^3,
\]
\[
F_j^\xi=-(\ell+\epsilon m_j(x_\xi))z_j^\xi-I_j^\xi+V_j(x_\xi),
\qquad F_s^\xi=-\ell_s s^\xi+\sum_jI_j^\xi,
\]
\[
Z_\xi^+=Z_\xi^-+hF_\theta(Z_\xi^+,x_\xi;w_t).
\]
其中 \(k\) 是基础传输系数，\(\ell,\ell_s\) 是泄漏系数，\(\epsilon\) 是固定输入调制强度；要求 \(h,k,\ell_s>0\)、\(\ell+\epsilon m_j(x_\xi)>0\)。\(h\) 是算术token步长，不是动作时长；\(w_t\) 可有符号，而 \(\alpha>0\)。在 \(\theta=0\) 时前向恢复原互易电流。泄漏和互易电流项耗散，但输入驱动可以供能，不能据此断言总能量单调下降。三次电流是抽象非线性传输模型，不是已确立的经典生理事实。

固定读出及归一化三路分差为
\[
RZ=((z_1-z_2)/2,\,(z_4-z_3)/2),\qquad
\hat D_{\xi|t}=\frac{RZ_\xi^+}{\|RZ_\xi^+\|},
\]
\[
f_t=\hat D_{t|t}^{T}\hat D_{A|t}-\hat D_{t|t}^{T}\hat D_{B|t}.
\]
每路要求 \(\|RZ_\xi^+\|>0\)，零描述子时未定义。三路使用同一版本的 \(\theta\)。因此参数写入不只影响当前分数，还会影响后续电位与敏感性；不能把门控视为仅在学习率上换一个系数，也不能把基点的导数永久沿用到非零参数。候选描述子因查询上下文而异，不能缓存成跨查询通用向量；每笔仍需三次前向、三路信用，并承担门控汇总和向候选广播的成本。

## 4. 后继相对证据与一次条件写入

候选 \(C\in\{A,B\}\) 来自查询后继到达前已完成的同动作记录，包含输入 \(x_C\)、已到达后继 \(y_C\) 和incoming；选择规则冻结，不能借未来观测挑候选。arrival只表示执行动作后观测确已到达，不表示抵达正确地点。令候选增量 \(v_C=y_C-x_C\)、查询增量 \(r_t=y_t-x_t\)，采用单位协方差Gaussian教师：
\[
\mu_C=x_t+v_C,\qquad \mathcal L_C=\mathcal N(y_t;\mu_C,I),
\]
\[
\Lambda_t=\log\frac{\mathcal L_A}{\mathcal L_B}
=\tfrac12\big(\|r_t-v_B\|^2-\|r_t-v_A\|^2\big).
\]
单位方差是当前无量纲接口假设。它比较哪条经验更能解释后继变化，不把同动作当作同位移或同地点，也尚未证明跨视角视觉可靠性。记 \(\sigma\) 为sigmoid，有精确恒等式
\[
\delta_t=\sigma(f_t+\Lambda_t)-\sigma(f_t),\qquad
L_{\rm ev}=-\log[\sigma(f_t)\mathcal L_A+(1-\sigma(f_t))\mathcal L_B],
\quad \partial_{f_t}L_{\rm ev}=-\delta_t.
\]
候选增量相同则相对证据与写入均为零。

令 \(\beta^\xi=\partial f_t/\partial Z_\xi^+\) 包含归一化及B路负号，\(J_\xi=\partial_ZF_\theta(Z_\xi^+,x_\xi;w_t)\)。隐式条件响应定义为
\[
(I_5-hJ_\xi)^T\lambda^\xi=\beta^\xi,
\]
\[
\mathrm{localCredit}_{t,j}^{\xi}
=-h\alpha_{t,j}w_{t,j}(d_j^\xi)^3(\lambda_j^\xi-\lambda_s^\xi),
\qquad
g_{t,j}=\sum_{\xi=t,A,B}\mathrm{localCredit}_{t,j}^{\xi}
=\partial_{\theta_j}f_t,\qquad
\Delta\theta_j=\eta\delta_tg_{t,j}.
\]
这里 \(\eta>0\) 是写入步长；局部资格同时依赖压差立方和支路—胞体误差差。星形耦合使 \(\lambda_s^\xi\) 传递该路的跨支路响应，并非把所有历史误差免费压成一个标量。以上梯度以incoming、\(w_t,C_*\) 和观测固定为条件，要求隐式解及响应可解；不回传产生incoming的全部历史，不是BPTT。

因果时序是：先冻结候选，以同版本参数计算并保存 \(f_t,g_t\) 及预测；实际后继到达才形成 \(\Lambda_t,\delta_t\)，核对版本后消费一次并写入；下一事件从在线状态继续，不能追改旧状态或用写入后的分差充当前次预测。连续事件链仍待验证。已有冻结骨干的有标签监督梯度缓存不属于这个无标签后继教师，不能拿来填补视觉接口。教学符号取决于实际到达证据，不由期望保护哪条关联反推。

## 5. 选择性对应及工程解释边界

在冻结基点，用 \(\nu,\omega\in\{0,1\}\) 标记两段经历仅供分析，定义
\[
K_{\nu\omega}=g_\nu^Tg_\omega,\qquad
\Delta f_\nu=\eta\delta_\omega K_{\nu\omega}+O(\|\Delta\theta\|^2),\qquad
S_{\rm cross}=\max\!\left(\frac{|K_{01}|}{K_{00}},\frac{|K_{01}|}{K_{11}}\right).
\]
最后一式要求自身项为正。这是局部写入近似：要减小交叉作用且保留自身写入，而非仅改变余弦符号。同号与反号证据四种关系都必须覆盖。共同/差分信用可能在固定四参数中形成可分别使用的方向，但并无任意历史都正交的保证。

此前各路独立门控会把候选自身历史的写入坐标混入当前关联，改变三路共同与差分项的平衡；暖候选原始分解显示，并非共同项简单压过差分项。新式让同一 \(w_t\) 作用于三路总信用，针对的是这一上下文不一致。它必须同时进入前向与导数，不是对旧梯度外接保护器。

系数生成精确满足 \(\log(\alpha/k)=\theta\odot w_t\)，属于上下文乘性门控；局部 \(K\) 可解释为门控切线特征，却不等价于整个隐式动态。生物结构的待证价值在于互易传输与完整响应的额外作用，而非门控名称：同门控简单自分流须获得同样候选重编码、参数、信息及写入预算；均值池有效也不能替胞体误差通路背书。

## 6. 证据范围与暂停位置

前一式在冷候选基点数值复现了约 \(0.020\) 的低交叉敏感性。当前共享门控式仅有基于保存基点的解析预言：三种配置 \(S_{\rm cross}\) 约为 \(0.021/0.024/0.024\)；这些不是新式实验结果，尚无对应实现和数值核验，不能据此宣布达到选择性目标。

仍未解决的是有限写入与连续压力下的稳健性、匹配自身改变量后相对同门控简单载体的额外价值，以及后继教师和真实视觉接口的有效性。局部低交叉、微量正差或不劣表现都不足以证明实用优势。区分真正减少串扰与少写入，仍需同时观察原始分差、自身修正、交叉回撤和保持量，并覆盖暖候选、两种写入顺序及四种证据关系；不能仅凭冷起态或一个有利符号组合归因。
